//We are settings an initial state here for clarity, probably is not needed

const Settings = {
    App: "VirtualOffice",
    Name: "virtualoffice",
    
    baseURl:"./",
  
};

export default Settings;
